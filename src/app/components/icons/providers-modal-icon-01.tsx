import type { SvgIconProps } from './svg-icon-props';

/** Renders the ProvidersModalIcon01 icon. */
export function ProvidersModalIcon01(props: SvgIconProps) {
  return (
    <svg {...props}>
      <path
        d="M6 6l12 12M18 6L6 18"
        stroke="currentColor"
        strokeWidth="1.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
